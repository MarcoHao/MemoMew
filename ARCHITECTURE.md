# MemoMew 架构设计

## 一、项目定位

MemoMew 是一个**AI驱动的轻量级个人知识库桌面应用**，核心定位是"带宠物管家的智能便笺"。

### 核心特性
- 🐱 **宠物形象化**：Canvas绘制的2D小橘猫，5种状态动画
- 🎙️ **语音交互**：Web Speech API语音输入/播报
- 🧠 **AI整理**：Kimi API自动摘要、标签、实体抽取
- 🕸️ **知识图谱**：D3.js力导向图可视化实体关系
- 💬 **智能问答**：基于笔记内容的RAG问答

---

## 二、技术选型

| 层级 | 技术 | 选择理由 |
|------|------|---------|
| **桌面框架** | Electron + Vite | 成熟的跨平台方案，开发速度快，打包生态完善 |
| **前端框架** | React 19 + TypeScript | 组件化开发，类型安全 |
| **样式方案** | Tailwind CSS v4 | 原子化CSS，快速搭建UI |
| **数据库** | better-sqlite3 | 零配置本地数据库，性能优异 |
| **AI引擎** | Kimi API (moonshot-v1) | 中文理解能力强，成本低 |
| **语音** | Web Speech API | 浏览器内置，完全免费 |
| **图谱可视化** | D3.js | 力导向图，成熟稳定 |
| **打包** | electron-builder | 一键出dmg/exe |

---

## 三、系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                      Electron 主进程                         │
│  ┌──────────────┐  ┌──────────────┐  ┌─────────────────────┐ │
│  │  better-sqlite3│  │  clipboard   │  │  globalShortcut     │ │
│  │  本地数据库    │  │  粘贴板监听   │  │  Cmd+Shift+O 唤起   │ │
│  └──────────────┘  └──────────────┘  └─────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────────────────────────────────────┐
│                     Chromium 渲染进程                        │
│  ┌──────────────┐  ┌──────────────┐  ┌─────────────────────┐ │
│  │  React UI    │  │  D3.js 图谱  │  │  Web Speech API     │ │
│  │  组件层      │  │  可视化      │  │  语音输入/播报      │ │
│  └──────────────┘  └──────────────┘  └─────────────────────┘ │
│  ┌──────────────┐  ┌──────────────┐  ┌─────────────────────┐ │
│  │  Kimi API    │  │  内存数据库  │  │  Canvas 宠物动画    │ │
│  │  AI 调用     │  │  (SQLite后备)│  │  状态机驱动         │ │
│  └──────────────┘  └──────────────┘  └─────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

---

## 四、数据模型

```
┌─────────────┐       ┌─────────────┐       ┌─────────────┐
│   notes     │       │  entities   │       │  relations  │
├─────────────┤       ├─────────────┤       ├─────────────┤
│ id          │       │ id          │       │ id          │
│ title       │◄──────│ name        │◄──────│ source      │
│ content     │       │ type        │       │ target      │
│ summary     │       │ description│       │ relation_type│
│ tags        │       │ note_ids    │       │ note_ids    │
│ created_at  │       │ created_at  │       │ created_at  │
└─────────────┘       └─────────────┘       └─────────────┘
```

---

## 五、核心流程

### 笔记创建流程
```
用户输入 → AI摘要+标签(Kimi) → AI实体抽取(Kimi) → 存储到SQLite
                                            ↓
                                    构建知识图谱节点和边
```

### 问答流程
```
用户提问 → 检索相关笔记 → 构建上下文 → Kimi API回答
                ↓
         宠物进入思考状态 → 宠物播报回答
```

---

## 六、开源依赖引用

| 项目 | 用途 | License |
|------|------|---------|
| Electron | 桌面应用框架 | MIT |
| React | UI框架 | MIT |
| Tailwind CSS | 样式方案 | MIT |
| D3.js | 知识图谱可视化 | ISC |
| better-sqlite3 | 本地数据库 | MIT |
| Lucide React | 图标库 | ISC |

---

## 七、目录结构

```
MemoMew/
├── electron/              # Electron主进程
│   ├── main.ts           # 主进程入口
│   ├── preload.ts        # 预加载脚本
│   └── db.ts             # SQLite数据库操作
├── src/
│   ├── api/              # API封装
│   │   ├── kimi.ts       # Kimi API
│   │   └── speech.ts     # Web Speech API
│   ├── components/       # React组件
│   │   ├── PetAvatar.tsx # 宠物动画
│   │   ├── Sidebar.tsx   # 侧边栏
│   │   ├── NoteEditor.tsx# 编辑器
│   │   ├── ChatPanel.tsx # 聊天面板
│   │   ├── GraphView.tsx # 知识图谱
│   │   └── SettingsModal.tsx # 设置
│   ├── db/               # 数据库封装
│   │   └── index.ts      # 内存/SQLite适配
│   ├── types/            # 类型声明
│   ├── App.tsx           # 主应用
│   ├── main.tsx          # 渲染入口
│   └── index.css         # 全局样式
├── docs/                 # 说明文档
├── package.json
├── tsconfig.json
├── vite.config.ts
└── index.html
```
